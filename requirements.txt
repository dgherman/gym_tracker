# Web framework
# Pinned: FastAPI 0.115.x bundles Starlette 0.40+, which uses the
# TemplateResponse(request, name, context) signature this code relies on.
fastapi>=0.115,<0.116
jinja2>=3.1,<4

# ASGI server
uvicorn[standard]>=0.22.0

# ORM & database drivers
SQLAlchemy>=1.4.0
alembic>=1.10.0
pymysql>=1.0.2
cryptography

# Data validation
pydantic>=1.9.0

# Testing
pytest>=7.0.0

# Auth
authlib>=1.3
python-dotenv>=1.0
itsdangerous>=2.1
httpx>=0.24